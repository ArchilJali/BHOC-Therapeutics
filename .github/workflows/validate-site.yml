name: Validate BHOC Therapeutics site

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - '**.html'
      - '**.css'
      - 'assets/**'
      - 'navigation-context.js'
      - 'robots.txt'
      - 'sitemap.xml'
      - 'bhoc/content-baseline.json'
      - 'scripts/**'
      - 'AGENTS.md'
      - '.github/workflows/validate-site.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-bhoc-site-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Protect approved BHOC content, routes and statuses
        run: node scripts/check_bhoc_content.mjs

      - name: Validate SEO, schema, sitemap, redirects and local links
        run: node scripts/check_site.mjs
